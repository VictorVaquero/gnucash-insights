name: CSP drift check

on:
  push:
    branches: [master]
  pull_request:

jobs:
  check-csp-drift:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - run: node scripts/check-csp-drift.mjs
